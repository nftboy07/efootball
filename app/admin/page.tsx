'use client';
import { useEffect, useState } from 'react';
const API=process.env.NEXT_PUBLIC_API_URL||'https://efootball-tournament.onrender.com';
type Tournament={id:string;name:string;status:string;players:any[];max_players:number;efootball_id?:string|null;bracket_generated?:boolean};
async function api(path:string,init?:RequestInit){const r=await fetch(API+path,init);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.detail||'Request failed');return d}
export default function Admin(){const [key,setKey]=useState('');const [name,setName]=useState('');const [code,setCode]=useState('');const [tournaments,setTournaments]=useState<Tournament[]>([]);const [selected,setSelected]=useState('');const [msg,setMsg]=useState('');const selectedTournament=tournaments.find(t=>t.id===selected);const headers=()=>({'Content-Type':'application/json','X-Admin-Key':key});async function load(){try{setTournaments(await api('/api/tournaments'));setMsg('')}catch(e:any){setMsg(e.message)}}useEffect(()=>{load()},[]);async function create(){try{const t=await api('/api/admin/tournaments',{method:'POST',headers:headers(),body:JSON.stringify({name})});setName('');setSelected(t.id);setMsg('Tournament created. Share its page link with players.');load()}catch(e:any){setMsg(e.message)}}async function action(path:string,body?:object){try{await api(path,{method:'POST',headers:headers(),body:body?JSON.stringify(body):undefined});setMsg('Updated successfully.');load()}catch(e:any){setMsg(e.message)}}return <div className="shell"><header className="nav"><a className="brand" href="/">eFootball<span>2026</span></a><span className="pill">ADMIN</span></header><main className="section admin-page"><div className="topline"><div><span className="eyebrow">TOURNAMENT CONTROL</span><h1>Run your community cup.</h1><p className="muted">Create a tournament, track all eight website registrations, then connect the KONAMI Custom Tournament room.</p></div></div><section className="card admin-key"><span className="eyebrow">SECURE ACCESS</span><h2>Admin key</h2><div className="field"><label>ADMIN_KEY</label><input type="password" value={key} onChange={e=>setKey(e.target.value)} placeholder="Enter your private admin key"/></div><p className="muted">This key is sent only to your API and is not saved.</p></section><div className="admin-layout"><section className="card"><span className="eyebrow">CREATE</span><h2>New tournament</h2><div className="field"><label>Tournament name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="eFootball 2026 Community Cup #1"/></div><button className="btn primary full" onClick={create} disabled={!key||name.trim().length<2}>CREATE TOURNAMENT</button>{msg&&<p className={msg.includes('success')||msg.includes('created')?'success':'error'}>{msg}</p>}</section><section className="card"><span className="eyebrow">LIVE REGISTRATION</span><h2>Player count</h2><button className="btn full" onClick={load}>REFRESH COUNTS</button><div className="admin-list">{tournaments.map(t=><button className={'admin-tournament '+(selected===t.id?'active':'')} key={t.id} onClick={()=>setSelected(t.id)}><span><strong>{t.name}</strong><small>{t.id} · {t.status}</small></span><b>{t.players.length}/{t.max_players}</b></button>)}{!tournaments.length&&<p className="muted">No tournaments created yet.</p>}</div></section></div>{selectedTournament&&<section className="card admin-actions"><div className="topline"><div><span className="eyebrow">SELECTED TOURNAMENT</span><h2>{selectedTournament.name}</h2><p className="muted">Share: {typeof window!=='undefined'?window.location.origin:''}/tournaments/{selectedTournament.id}</p></div><div className="count-badge">{selectedTournament.players.length}<span>/8 PLAYERS</span></div></div><div className="admin-progress"><i style={{width:`${selectedTournament.players.length/8*100}%`}}/></div><div className="player-list">{selectedTournament.players.map((p:any,i:number)=><div className="playerrow" key={p.id}><span>{i+1}. {p.display_name}</span><span className="muted">{p.efootball_username}</span></div>)}{!selectedTournament.players.length&&<p className="muted">Waiting for players to register.</p>}</div><div className="admin-controls"><button className="btn" disabled={selectedTournament.players.length!==8||!!selectedTournament.bracket_generated} onClick={()=>action(`/api/admin/tournaments/${selectedTournament.id}/bracket`)}>GENERATE BRACKET</button><div className="field inline-field"><label>KONAMI TOURNAMENT ID</label><input value={code} onChange={e=>setCode(e.target.value)} placeholder="0004-6470-6202"/></div><button className="btn primary" disabled={selectedTournament.players.length!==8||!selectedTournament.bracket_generated||code.trim().length<1} onClick={()=>action(`/api/admin/tournaments/${selectedTournament.id}/efootball-id`,{tournament_id:code})}>ACTIVATE ROOM</button></div>{selectedTournament.efootball_id&&<div className="notice">KONAMI room active: <strong>{selectedTournament.efootball_id}</strong></div>}</section>}</main><footer className="footer">Admin access is protected by your API ADMIN_KEY.</footer></div>}

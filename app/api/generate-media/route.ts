import { NextRequest, NextResponse } from 'next/server';
const hfModel='black-forest-labs/FLUX.1-schnell';
export async function POST(request:NextRequest){const body=await request.json().catch(()=>null);if(!body||typeof body.prompt!=='string'||body.prompt.trim().length<10)return NextResponse.json({error:'Enter a descriptive prompt.'},{status:400});const prompt=encodeURIComponent(body.prompt.trim());const provider=body.provider||'pollinations-image';try{if(provider==='pollinations-image')return NextResponse.json({media:{url:`https://image.pollinations.ai/prompt/${prompt}?width=1080&height=1920&nologo=true`,contentType:'image/jpeg'}});if(provider==='hf-image'){const token=process.env.HF_TOKEN;if(!token)return NextResponse.json({error:'HF_TOKEN is not configured.'},{status:503});const r=await fetch(`https://router.huggingface.co/hf-inference/models/${hfModel}`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({inputs:body.prompt.trim(),parameters:{width:768,height:1365}})});if(!r.ok){const error=await r.text();throw new Error(`Hugging Face image request failed (${r.status}): ${error.slice(0,240)}`)}const data=await r.arrayBuffer();return NextResponse.json({media:{url:`data:image/png;base64,${Buffer.from(data).toString('base64')}`,contentType:'image/png'}})}if(provider==='vercel-image')return NextResponse.json({error:'Vercel image generation is not available in this build.'},{status:501});return NextResponse.json({error:'Unsupported generation provider.'},{status:400})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Media generation failed'},{status:502})}}

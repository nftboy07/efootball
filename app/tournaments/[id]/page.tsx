'use client';
import { useEffect, useState } from 'react';
import { useParams } from 'next/navigation';
const API=process.env.NEXT_PUBLIC_API_URL||'https://efootball-tournament-kwq4.onrender.com';
async function api(path:string,init?:RequestInit){const r=await fetch(API+path,init);const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.detail||'Request failed');return d}
const stages=[{round:'QF',name:'Quarterfinals',note:'8 → 4'},{round:'SF',name:'Semifinals',note:'4 → 2'},{round:'F',name:'Grand final',note:'2 → 1'}];
export default function Tournament(){const {id}=useParams<{id:string}>();const [t,setT]=useState<any>();const [name,setName]=useState('');const [ef,setEf]=useState('');const [msg,setMsg]=useState('');const [matches,setMatches]=useState<any[]>([]);const load=()=>{setMsg('');api('/api/tournaments/'+id).then(setT).catch(e=>setMsg(e.message));api('/api/tournaments/'+id+'/matches').then(setMatches).catch(()=>{})};useEffect(()=>{load()},[id]);async function join(){setMsg('');try{const d=await api('/api/tournaments/'+id+'/players',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({display_name:name,efootball_username:ef})});localStorage.setItem('player_token',d.player.token);setMsg('success: Registration confirmed. Keep your player token private.');load()}catch(e:any){setMsg('error: '+e.message)}}if(!t)return <div className="matchday-shell"><header className="matchday-nav"><a className="brand-lockup" href="/"><span className="brand-mark">eF</span><span>eFootball <b>2026</b></span></a></header><main className="section missing-tournament"><div className="status-note"><span className="section-index">MATCHDAY ERROR</span><h2>{msg||'Loading tournament…'}</h2>{msg&&<><p>This link is unavailable. The organizer may have restarted the tournament service.</p><a className="matchday-button primary" href="/#tournaments">View live cups <span>↗</span></a><button className="text-link" onClick={load}>Try again</button></>}</div></main></div>;return <div className="matchday-shell"><header className="matchday-nav"><a className="brand-lockup" href="/"><span className="brand-mark">eF</span><span>eFootball <b>2026</b></span></a><span className="cup-status"><i/>{t.status}</span></header><main><section className="tournament-hero"><div><span className="section-index">COMMUNITY CUP / {t.id}</span><h1>{t.name}<br/><em>Matchday.</em></h1><p>{t.players.length}/8 registered players · Free entry · Follow the route to the final.</p></div><div className="tournament-score"><span>PLAYER SLOTS</span><strong>{String(t.players.length).padStart(2,'0')}<small>/08</small></strong><div className="progress-track"><i style={{width:`${Math.min(100,t.players.length/t.max_players*100)}%`}}/></div><b>{t.efootball_id?'ROOM ACTIVE':'ROOM PENDING'}</b></div></section><section className="section tournament-content"><div className="route-track tournament-route">{stages.map((stage,index)=><div key={stage.round}><b>{stage.round}</b><strong>{stage.name}</strong><span>{stage.note} players</span>{index<stages.length-1&&<i/>}</div>)}</div>{t.efootball_id&&<section className="code-panel"><span className="section-index">KONAMI CUSTOM ROOM</span><strong>{t.efootball_id}</strong><p>Open eFootball → Custom Tournament → Join Tournament and enter this code.</p></section>}<div className="tournament-columns"><section><div className="section-heading compact"><div><span className="section-index">REGISTERED ROSTER</span><h2>Players<br/><em>in the cup.</em></h2></div><p>Website registrations are counted here. The KONAMI lobby itself must be checked in-game.</p></div><div className="roster">{t.players.map((p:any,i:number)=><div className="roster-row" key={p.id}><b>0{i+1}</b><strong>{p.display_name}</strong><span>{p.efootball_username}</span></div>)}{!t.players.length&&<div className="ranking-empty">The first player opens the roster.</div>}</div></section><section className="join-panel"><span className="section-index">RESERVE A SLOT</span><h2>Join the<br/><em>matchday.</em></h2>{t.players.length>=8?<div className="status-note">This cup is full. Watch the main page for the next opening.</div>:<form onSubmit={e=>{e.preventDefault();join()}}><div className="field"><label htmlFor="display-name">Display name</label><input id="display-name" autoComplete="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></div><div className="field"><label htmlFor="efootball-name">eFootball username</label><input id="efootball-name" spellCheck="false" value={ef} onChange={e=>setEf(e.target.value)} placeholder="Your eFootball name"/></div><button className="matchday-button primary full" type="submit">Reserve slot <span>↗</span></button></form>}{msg&&<p className={msg.startsWith('success')?'success':'error'} role="status">{msg}</p>}</section></div><section className="matches-section"><span className="section-index">LIVE BRACKET</span><h2>Match <em>route.</em></h2><div className="match-list">{matches.map(m=><div className="match-row" key={m.id}><span>{m.round} / {m.slot}</span><strong>{m.player_a||'TBD'}</strong><b>{m.score_a??'—'}</b><strong>{m.player_b||'TBD'}</strong><b>{m.score_b??'—'}</b><small>{m.status}</small></div>)}{!matches.length&&<div className="ranking-empty">The bracket appears after all eight slots are filled.</div>}</div></section></section></main><footer className="matchday-footer"><span>eFootball <b>2026</b></span><span>Independent community platform · Not affiliated with KONAMI.</span><a href="/#tournaments">Back to cups ↗</a><span className="social-links"><a href="https://www.instagram.com/efbt__2026/" target="_blank" rel="noreferrer" aria-label="Follow eFootball 2026 on Instagram">Instagram ↗</a><a href="https://www.facebook.com/search/pages/?q=efbt__2026" target="_blank" rel="noreferrer" aria-label="Find eFootball 2026 on Facebook">Facebook ↗</a></span></footer></div>}
